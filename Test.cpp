#include "doctest.h"
#include "FamilyTree.hpp"

#include <string>
#include <iostream>
using namespace std;

TEST_CASE("Test Adding Father / Mother") {
	CHECK( );
	CHECK_THROWS_AS(( ), std::exception);
}

TEST_CASE("Test relation / find") {
	CHECK( );
	CHECK_THROWS_AS(( ), std::exception);
}

TEST_CASE("Test remove") {
	CHECK( );
	CHECK_THROWS_AS(( ), std::exception);
}